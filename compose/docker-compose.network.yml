networks:
  admin-net:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: "172.28.10.0/24"
          gateway: "172.28.10.1"

  homeassistant-net:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: "172.28.20.0/24"
          gateway: "172.28.20.1"
         
  monitoring-net:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: "172.28.30.0/24"
          gateway: "172.28.30.1"

  registry-net:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: "172.28.40.0/24"
          gateway: "172.28.40.1"
